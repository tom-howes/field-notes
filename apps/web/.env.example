VITE_API_BASE_URL=http://127.0.0.1:3000

# Optional: shown as a "use a demo account" reveal on the sign-in screen, so
# recruiters can try the app without spending one of the 5 allowlist slots
# Spotify's Development Mode gives us. Leave unset to hide the option entirely.
VITE_DEMO_SPOTIFY_EMAIL=
VITE_DEMO_SPOTIFY_PASSWORD=
